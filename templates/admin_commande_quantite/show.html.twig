{% extends 'base.html.twig' %}

{% block title %}CommandeQuantite{% endblock %}

{% block body %}
    <h1>CommandeQuantite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_quantite.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ commande_quantite.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_commande_quantite_index') }}">back to list</a>

    <a href="{{ path('admin_commande_quantite_edit', {'id': commande_quantite.id}) }}">edit</a>

    {{ include('admin_commande_quantite/_delete_form.html.twig') }}
{% endblock %}
